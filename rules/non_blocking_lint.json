{
  "protocol_name": "Non-blocking Lint",
  "description": "Periodic wiki health-check mechanism that does not block agent operations.",
  "entry_points": {
    "manual": "./scripts/lint.sh [--quiet] [--skip-checks ID1,ID2]",
    "cron": "0 */4 * * * cd /path/to/loomana && ./scripts/lint.sh --quiet >> logs/lint.log 2>&1"
  },
  "output_format": {
    "stdout": "JSON report",
    "stderr": "human-readable summary"
  },
  "checks_summary": "Includes contradiction checks, orphan pages, knowledge gaps, new sources, mechanical errors, date consistency, broken links, deep contradiction scanning, textual similarity and stale hot cache.",
  "schema_ref_to_agent_rules": "AGENTS.md#auto-rebuild-metadata-meta-rebuild-path"
}
