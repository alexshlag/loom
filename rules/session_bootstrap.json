{
  "name": "session-bootstrap",
  "trigger": "session_start",
  "description": "Order-independent, explicit file load sequence for session start. Agent MUST execute all steps in order.",
  "steps": [
    {
      "order": 1,
      "action": "read_file",
      "path": "working_memory.json"
    },
    {
      "order": 2,
      "action": "read_file",
      "path": "wiki/hot.md"
    },
    {
      "order": 3,
      "action": "read_file",
      "path": "rules/git_conventions.json",
      "note": "MUST read before any commit operation — defines staging mode, commit format, memory sync rules"
    },
    {
      "order": 4,
      "action": "scan_directory",
      "path": "wiki/skills/*-skill.md",
      "output": "working_memory.skills_loaded = [{name, category}]",
      "note": "Non-blocking — failure does not abort session. Skills loaded into WM for agent awareness during processes.",
      "schema_ref": "rules/skill_format.json#category_taxonomy"
    }
  ]
}
