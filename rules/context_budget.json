{
  "rule_id": "CBUDGET-V2",
  "rule_name": "context_budget",
  "lazy_loading": {
    "rule_id": "CB-LAZY",
    "principle": "Process files are schema_refs — read specific step when needed.",
    "examples": [
      {"when": "only step_8a needed", "action": "read process-ingest.json#step_8a_new_page"},
      {"when": "only step_1.5 needed", "action": "read process-query.json#step_1.5"}
    ]
  },

  "source_lifecycle": {
    "rule_id": "CB-SOURCE",
    "principle": "One source at a time: read → extract → write wiki → close.",
    "prohibited": "Holding ≥2 source texts simultaneously during batch ingest"
  },

  "context_bubble": {
    "rule_id": "CB-BUBBLE",
    "max_open_wiki_pages": 3,
    "rule": "When opening N+1th wiki page → close one of current N first."
  },

  "schema_ref": {
    "context_bubble_detail": "rules/session_context_rules.json#operational_rules.context_bubble_max_pages"
  }
}
