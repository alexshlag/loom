{
  "rule_id": "NAMES-CORE-V1",
  "name": "Wiki page naming conventions — project prefixing",
  "version": "1.0",
  "date": "2026-07-07",
  "description": "Prevent filename collision when multiple projects/frameworks share wiki. All pages must be prefixed with project name (except truly abstract concepts).",
  "schema_ref_to_agent_rules": "AGENTS.md#architecture-layers",
  "rules": {
    "entities_directory": {
      "path": "wiki/entities/",
      "pattern": "<project-slug>-<entity-type>.md",
      "examples": [
        { "file": "ai-factory.md", "type": "cli-tool" },
        { "file": "symfony-messenger.md", "type": "component-library" },
        { "file": "nodejs.md", "type": "runtime" }
      ],
      "rule": "Always prefix with project/framework name. Never use bare concept names (e.g., messenger-component.md is WRONG, symfony-messenger.md is CORRECT)."
    },
    "concepts_directory": {
      "path": "wiki/concepts/",
      "patterns": [
        {
          "pattern": "abstract-only",
          "description": "Truly abstract concepts that apply across all projects — no prefix needed.",
          "examples": [
            { "file": "cache-system.md", "type": "architecture-pattern" },
            { "file": "hexagonal-architecture.md", "type": "design-pattern" }
          ],
          "rule": "Only use bare names when concept is genuinely framework-agnostic. If it's a specific implementation (e.g., Messenger Component), prefix with project name: symfony-messenger-component.md"
        },
        {
          "pattern": "project-specific",
          "description": "Concepts tied to a specific project/framework — always prefix.",
          "examples": [
            { "file": "symfony-messaging-pattern.md", "type": "framework-concept" },
            { "file": "react-hooks-pattern.md", "type": "framework-concept" }
          ],
          "rule": "Always prefix with project name. Use hyphenated lowercase kebab-case."
        }
      ]
    },
    "docs_directory": {
      "path": "wiki/docs/",
      "pattern": "<project-slug>-<doc-type>.md",
      "examples": [
        { "file": "ai-factory.md", "type": "cli-tool-docs" },
        { "file": "symfony-cli-guide.md", "type": "framework-documentation" }
      ],
      "rule": "Always prefix with project name. Never use bare concept names in docs/."
    },
    "comparisons_directory": {
      "path": "wiki/comparisons/",
      "pattern": "<project-a>-vs-<project-b>.md",
      "examples": [
        { "file": "ai-factory-vs-pi.md", "type": "cross-comparison" },
        { "file": "symfony-vs-laravel-messaging.md", "type": "framework-comparison" }
      ],
      "rule": "Compare two entities: both names must be present in the filename."
    }
  },
  "aliases_system": {
    "primary_name_rule": "Choose most common/official name as primary filename. Use less common variants in aliases frontmatter field.",
    "example": { "file": "symfony-messenger.md", "aliases": ["messenger-component", "messaging-bus"] },
    "rule": "Aliases stored in YAML frontmatter `aliases: []` array — never create separate files for alternative names."
  },
  "injection_prevention": {
    "allowed_chars": "lowercase-letters, digits, hyphens, underscores (kebab-case preferred)",
    "prohibited_patterns": [
      { "pattern": "..", "reason": "path traversal" },
      { "pattern": "spaces or special chars", "reason": "filesystem safety" }
    ],
    "rule": "sanitize file names with validate-path.sh before writing."
  }
}
