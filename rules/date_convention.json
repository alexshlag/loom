{
  "protocol_name": "Date Convention Rule",
  "description": "Date usage rules for metadata and logs.",
  "rules": {
    "frontmatter_date": "Current system date in YYYY-MM-DD format. Never derived from source filename or git commit dates.",
    "log_entries": "## [YYYY-MM-DD] action | description",
    "json_timestamps": "ISO-8601 YYYY-MM-DDTHH:MM:SS+TZ"
  },
  "enforcement": "lint.sh check_id=6 (date_consistency_check)",
  "schema_ref_to_agent_rules": "AGENTS.md#date-convention-rule"
}
